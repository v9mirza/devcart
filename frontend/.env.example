# Copy to .env.local for local overrides (optional — defaults to http://localhost:5000):
#   cp .env.example .env.local

# Backend API base URL (no trailing slash)
# VITE_API_URL=http://localhost:5000
